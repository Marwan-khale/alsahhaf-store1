# styles/

Global stylesheet lives at `app/globals.css` (Next.js App Router convention).
This folder is reserved for additional, non-global stylesheets if a future
approved feature requires them (e.g. print stylesheets). Empty by design.
